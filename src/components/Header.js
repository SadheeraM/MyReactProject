import React from 'react';
import SideBar from "./SearchBar";

const Header = () => {
    return (
        <header>
            <h1>Fancy Books</h1>
            <SideBar/>
        </header>
    );
};

export default Header;
